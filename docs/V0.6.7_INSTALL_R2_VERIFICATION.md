# 0.6.7 安装说明 r2 交付验证

2026-09-07，macOS 26.5.1 / arm64。

- 当前维护分支的 `npm test`：147 项通过，无失败、无跳过；静态与版本检查通过。
- 以原已发布 ZIP 为输入，SHA-256 为 `0eec1a4ddf0e75b3b467b39e1300dacd053aa7b613b21e2a03f77217d5a37b95`；插件仍为标签 `v0.6.7-internal` 的 0.6.7，不包含后续单行工具栏修改。
- 184 个非修订文档文件、25 个目录的路径、内容与权限保持不变，包括全部 178 个扩展文件及安装、卸载脚本。
- 修订 ZIP 完整性检查通过；DMG 内部校验通过；DMG 只读挂载后全部交付内容与 ZIP 一致，已卸载临时磁盘。
- 从修订 ZIP 解压执行真实脚本，在临时用户目录完成首次安装、再次升级、同 ID 唯一性、缺少 ffprobe 时不覆盖已有扩展、卸载及可恢复备份验证。
- 使用本机真实 ffmpeg/ffprobe；跳过当前用户 PlayerDebugMode 写入，没有修改实际 Adobe 安装目录，没有启动用户 Adobe 工程。
- 没有 Windows、Intel、其他 macOS/Adobe 组合或零基础真人试装结果，不宣称“所有系统直接安装”。

| 附件 | SHA-256 |
|---|---|
| File-Bridge-0.6.7-macOS-install-r2.zip | `49a570d72d3e153703354e4d606bd5cb8e5ea8c396b3060e00e83c75cb60972c` |
| File-Bridge-0.6.7-macOS-install-r2.dmg | `caf9c9aea354bfb4a6b3cdb6d5bd071a792d528642dd4d879fcf3f542122919c` |
| File-Bridge-0.6.7-macOS-Install-Guide.txt | `f5432036df4dc3fbc73b144ad73e40b1222a36f55d03119619bbbe0e661ca9cb` |

`verification.json` 是封装工具的包校验记录；其中安装测试标记为由独立步骤执行。
实际安装、升级和卸载的结果在 `installer-verification.json`，两份记录均在 Release 附件，
并列入 `SHA256SUMS-0.6.7-install-r2.txt`。

旧 ZIP、DMG 和旧校验文件仍保留于桌面，未删除原始交付文件。
