# 使用 AI 修改 Rove 的检查清单

每次只给 AI 一个明确目标，并要求它遵守 `AGENTS.md`。

开始前：

- 说明期望行为、当前行为和复现步骤。
- 让 AI 先读取相关代码，不要直接重写整个项目。
- 从 `main` 新建独立分支。

完成前：

- 查看 `git diff`，确认没有不相关文件。
- 运行 `npm test`。
- 在 Adobe 中实际操作一次受影响流程。
- 检查是否新增网络请求、文件写入、删除行为或敏感信息。
- 要求 AI 用普通语言总结“改了什么、怎么验证、还有什么风险”。

