# LK‘s File Bridge v0.6 迭代验收表

本轮覆盖意见 82–112。版本：`0.6.4`。基础浏览和播放器修复继续沿用 `V5_ACCEPTANCE.md`。

| 编号 | 状态 | 结果 |
|---:|:---:|---|
| 82 | 通过 | 右键插入、转码、插件文件夹子菜单增加无缝 hover bridge，移入父项即可展开。 |
| 83–85 | 通过 | 缩放条去掉两端装饰圆点，滑块圆点随数值变化，轨道 2px 并保持中心对齐。 |
| 86–88 | 通过 | 置顶改为图钉 CSS 图标，使用轻微中性白色外框强调，不使用暖色高亮。 |
| 89 | 通过 | 列表模式缩略图显式 `align-self:center`，固定行高内上下居中。 |
| 90–91 | 通过 | 上传和新建本地文件夹完成后会重新扫描、选中目标并滚动到目标卡片；插件文件夹创建也会自动选中。 |
| 92–94 | 通过 | 缩放条移到左工具栏，右侧文件夹入口打开从顶部下拉的半屏素材位置面板；路径按单列长条列表呈现，并提供返回、关闭、全选和添加位置操作。 |
| 95–97 | 通过 | 音量条常显，扬声器和循环图标重绘。 |
| 98 | 通过 | 卡片缩放到约 30% 时隐藏名称、格式、收藏、标签、Proxy、置顶等信息。 |
| 99 | 通过 | 全选按钮改为进入/退出勾选模式，首次点击不会自动全选。 |
| 100–101 | 部分通过 | 导入/插入和拖拽前置导入会向 Premiere ProjectItem 写入插件色彩标签；映射覆盖 Premiere 16 色索引。时间线直接拖放的标签同步依赖宿主完成导入回调。 |
| 102 | 通过 | 海报、雪碧图、波形和元数据按源文件路径/大小/修改时间缓存到本机，下次打开复用缓存。 |
| 103–104 | 通过 | 使用程序生成的无版权 Log 风景参考片，LUT 画布自适应占满预览区。 |
| 105 | 通过 | 本地扫描不再生成文件夹卡片，只保留递归找到的文件；插件文件夹独立持久化，不移动本地文件，支持创建、重命名、删除、拖入和右键归类，并可返回上一层。 |
| 105A | 通过 | 根目录按“文件夹区 / 文件区”分组；插件文件夹显示内部素材拼盘或空文件夹图标和项目数量，进入后支持子文件夹与可见返回条。 |
| 106–107 | 部分通过 | Premiere 宿主提供 Lumetri Color + Input LUT 的受保护应用尝试；无选中视频时按钮置灰并显示“当前时间线未选中”。不同 Premiere 版本若无可写属性会返回明确不支持。 |
| 108 | 部分通过 | LUT 拖拽携带专用 MIME，并在 Premiere 宿主尝试应用到当前选中视频；CEP 无法监听宿主时间线 drop 回调时保留明确错误反馈。 |
| 109 | 通过 | 移除 Canvas 技术说明段落。 |
| 110–112 | 通过 | 增加切换对比、分割对比、滑动对比三种模式；分割线支持指针拖动，应用按钮和状态反馈重新布局。 |

## 回归

- Node 测试：`64/64` 通过；新增文件夹层级、拼盘封面和返回控件契约检查，并保留选中视频内嵌播放回归。
- 浏览器：813px 压力目录、280–1200px 响应式、插件文件夹分组/拼盘/空文件夹/嵌套返回、选择模式、LUT 查看器和列表模式通过。
- Premiere 宿主：空选择 LUT 错误路径、截图/标签/项目清单接口通过脚本模拟。
