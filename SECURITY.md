# Security Policy

## 敏感信息

仓库不得包含密码、API 密钥、访问令牌、签名证书、NAS 登录信息或用户素材。发现泄露后，应先撤销对应凭据，再从 Git 历史中清理。

## 报告安全问题

请不要在公开 Issue 中披露可利用的漏洞或凭据。仓库启用 GitHub Private Vulnerability Reporting 后，优先通过该入口报告；在启用前，由仓库所有者通过私下渠道处理。

## 支持范围

开发中的 `0.x` 版本不承诺长期兼容。正式发布后，只为最新稳定版本提供安全修复。

